# Compatibility pointer

The active plan is stored at [`../plans/mounted-fuse-write-profile-plan.md`](../plans/mounted-fuse-write-profile-plan.md).

This pointer exists only so relative links inside preserved historical documents remain valid.
