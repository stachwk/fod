# msfind requests for fod-indexer

This file collects functionality that `msfind` wants from `fod-indexer` instead of building a separate indexing engine in `msfind`.

Add entries as short bullets:
- what is missing
- why `msfind` needs it
- expected behavior or constraints
- links to the related `msfind` work, if useful

## Requests
